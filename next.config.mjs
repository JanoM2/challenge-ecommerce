/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "api.api-ninjas.com",
      "api.dicebear.com",
      "s3.amazonaws.com",
      "png.pngtree.com",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
